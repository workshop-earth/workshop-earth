<section id="{{id}}" class="section">
	<h2 class="section-title">{{name}}</h2>
	<p class="section-intro">{{intro}}</p>
	{{#if project-url}}
	<a class="button button-primary section-link" href="{{project-url}}" target="_blank">Launch {{name}}</a>
	{{/if}}
</section>